import { averageWait } from '../../../helpers/average-wait';
import { module, test } from 'qunit';

module('Unit | Helper | average wait');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = averageWait(42);
  assert.ok(result);
});
