import { eachIn } from '../../../helpers/each-in';
import { module, test } from 'qunit';

module('Unit | Helper | each in');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = eachIn(42);
  assert.ok(result);
});
